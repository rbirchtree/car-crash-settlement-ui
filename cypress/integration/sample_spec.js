describe('App', () => {
    beforeEach(() => {
        cy.visit('/')
    })

    it('visits the app', () => {
        cy.visit('/')
    })


    it('visits the about', () => {
        cy.visit('/about')
    })

    

})